# Copy this file to scanner.env (already the name systemd expects) and
# fill in your real values. Keep this file readable only by you:
#   chmod 600 scanner.env

FINNHUB_API_KEY=paste_your_free_finnhub_key_here

# Local path to a git clone of your Stock-Stalker repo, with push access
# already working from this Pi (test with a manual `git push` first).
REPO_DIR=/home/connor/Stock-Stalker
